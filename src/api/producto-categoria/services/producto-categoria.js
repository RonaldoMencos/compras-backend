'use strict';

/**
 * producto-categoria service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::producto-categoria.producto-categoria');
