'use strict';

/**
 * producto-categoria router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::producto-categoria.producto-categoria');
