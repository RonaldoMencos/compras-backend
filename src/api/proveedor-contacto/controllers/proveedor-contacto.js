'use strict';

/**
 * proveedor-contacto controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::proveedor-contacto.proveedor-contacto');
