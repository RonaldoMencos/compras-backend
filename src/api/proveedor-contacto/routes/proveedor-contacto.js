'use strict';

/**
 * proveedor-contacto router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::proveedor-contacto.proveedor-contacto');
