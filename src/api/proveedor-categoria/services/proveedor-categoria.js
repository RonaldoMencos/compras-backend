'use strict';

/**
 * proveedor-categoria service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::proveedor-categoria.proveedor-categoria');
