'use strict';

/**
 * proveedor-categoria router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::proveedor-categoria.proveedor-categoria');
