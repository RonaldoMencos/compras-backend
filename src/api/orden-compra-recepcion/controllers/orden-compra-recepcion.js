'use strict';

/**
 * orden-compra-recepcion controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::orden-compra-recepcion.orden-compra-recepcion');
