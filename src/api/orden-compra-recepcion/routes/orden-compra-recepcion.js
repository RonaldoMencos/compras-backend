'use strict';

/**
 * orden-compra-recepcion router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::orden-compra-recepcion.orden-compra-recepcion');
